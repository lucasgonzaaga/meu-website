import React from 'react';

const Works = () => {
    return (
        <section className="min-h-screen w-full flex items-center justify-center bg-black text-white p-10">
            <h2 className="text-4xl font-bold">My Works</h2>
        </section>
    );
};

export default Works;
